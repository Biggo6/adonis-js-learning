<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>Home</title>
</head>
<body class="container" style="margin-top: 200px">

        @if(flashMessage('notification'))
            <span class="text-info mb-12">
                {{ flashMessage('notification') }}
            </span>
        @endif

        <form action="{{ route('TodoController.store')}}" method="POST">
            {{ csrfField() }}
            <input type="text" name="text" class="mb-3 mt-20 form-contol" />  
        </form>  
        @if(hasErrorFor('text'))
                <span class="text-danger">
                    {{ getErrorFor('text') }}
                </span>
        @endif
        <ul style="margin-top: 32px" class="list-group list-group-flush">
            @each(todo in todos)
                <li class="list-group-item">{{todo.text}}
                <a href="{{ route('TodoController.destroy', { id: todo.id }) }}"><span class="badge badge-danger float-right"> Delete </span></a>
                    <span class="badge badge-info float-right mr-2"> Edit </span>
                </li>
            @endeach
        </ul>    
</body>
</html>